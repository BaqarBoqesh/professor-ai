name: Keep Streamlit Alive

on:
  schedule:
    - cron: '0 */12 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s https://profeappr-ai-cnkgabtrerxgqz2s28ihr6.streamlit.app/ > /dev/null
